#!/usr/bin/env groovy

node { // <1>
    stage('Build') {
        /* .. snip .. */
    }
    stage('Test') {
        /* .. snip .. */
    }
    stage('Deploy') {
        /* .. snip .. */
    }
    stage('Finished') {
        /* .. snip .. */
    }
}