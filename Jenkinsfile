#!groovy

node { // <1>
    stage('Check out') {
        checkout scm
    }
    stage('Build') {
        /* .. snip .. */
    }
    stage('Test') {
        /* .. snip .. */
    }
    stage('Deploy') {
        /* .. snip .. */
    }
    stage('Finished') {
        /* .. snip .. */
    }
}