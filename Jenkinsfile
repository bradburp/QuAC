#!/usr/bin/env groovy

node { // <1>

    stage('Build') {
        System.getProperty("user.dir");
    }
    stage('Test') {
        /* .. snip .. */
    }
    stage('Deploy') {
        /* .. snip .. */
    }
    stage('Finished') {
        /* .. snip .. */
    }
}