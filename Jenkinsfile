#!groovy

node { // <1>

    stage('Build') {
        sh 'cat /etc/passwd'
    }
    stage('Test') {
        /* .. snip .. */
    }
    stage('Deploy') {
        /* .. snip .. */
    }
    stage('Finished') {
        /* .. snip .. */
    }
}